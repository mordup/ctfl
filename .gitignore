__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.eggs/
*.so

# Virtual environments
.venv/
venv/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Distribution
# Regenerated by scripts/release.sh on every AppImage build (an absolute path
# to the freshly-built wheel, which python-appimage requires), so tracking it
# just dirties the tree on any checkout that isn't the author's.
appimage/requirements.txt
*.tar.gz
*.tar.zst
*.whl
*.deb
*.rpm
*.AppImage
pkg/
src/

# Claude
CLAUDE.md
.claude/settings.local.json
.claude/agent-memory/
# Workflow scripts are written here by the Workflow tool on every run. Unlike
# the hand-authored agents/skills/commands above, they are generated copies of
# bundled skills and go stale as those update.
.claude/workflows/
